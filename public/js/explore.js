// public/js/explore.js

document.addEventListener("DOMContentLoaded", function () {
  // Optional future enhancement: collapse sections, search filter, etc.

  // For now, just logs
  console.log("Explore BYS JS Loaded ✅");

  // Example: Auto-collapse unused sections (future)
  // document.querySelectorAll(".section").forEach(section => {
  //   section.classList.add("collapsed");
  // });

  // Highlight role badges (already styled via CSS)
});
