<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= kender.name %> Kender Directory</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 30px; }
    h1, h2, h3 { color: #2c3e50; }
    .section { margin-bottom: 30px; }
    .team-member { margin-bottom: 8px; }
  </style>
</head>
<body>
  <h1>🧘‍♂️ Bhartiya Yog Sansthan</h1>
  <h2><%= kender.name %> Kender Directory</h2>
  <p><strong>Ksheter:</strong> <%= kender.ksheter.name %> | <strong>Zila:</strong> <%= kender.zila.name %></p>
  <% if (kender.address) { %><p><strong>Address:</strong> <%= kender.address %></p><% } %>
  <% if (kender.mapLink) { %><p><strong>Map:</strong> <%= kender.mapLink %></p><% } %>
  <p><strong>Date:</strong> <%= new Date().toLocaleDateString() %></p>

  <div class="section">
    <h3>👥 Kender Team</h3>
    <% if (team.length === 0) { %>
      <p>No team members found.</p>
    <% } else { %>
      <% team.forEach(member => { %>
        <div class="team-member">
          <strong><%= member.name %></strong> — <%= member.role.join(', ') %> — 📞 <%= member.mobile %>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>
