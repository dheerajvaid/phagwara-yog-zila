<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Team Attendance Rank Report</title>
  <style>
    body {
      font-family: 'Segoe UI', Arial, sans-serif;
      font-size: 17px;
      color: #2c3e50;
      background-color: #fff;
      margin: 0;
      padding: 20px;
    }

    .header-logo {
      text-align: center;
      margin-bottom: 15px;
    }

    .header-logo img {
      max-height: 90px;
    }

    h2 {
      text-align: center;
      font-size: 26px;
      font-weight: bold;
      margin: 10px 0 8px;
      color: #b22222;
    }

    .kender-name {
      text-align: center;
      font-size: 20px;
      font-weight: 600;
      margin-bottom: 20px;
      color: #1e88e5;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      font-size: 18px;
    }

    th, td {
      border: 1px solid #999;
      padding: 12px 8px;
      text-align: center;
    }

    th {
      background-color: #dc3545;
      color: #fff;
      font-weight: 700;
      font-size: 18px;
    }

    tr:nth-child(even) {
      background-color: #f5f5f5;
    }

    .footer {
      margin-top: 30px;
      text-align: right;
      font-size: 15px;
      color: #555;
    }
  </style>
</head>
<body>

  <div class="header-logo">
    <img src="<%= logoBase64 %>" alt="Bharatiya Yog Sansthan Logo">
  </div>

  <h2>Team Attendance Rank Report<br>
    <%= new Date(selectedYear, selectedMonth - 1).toLocaleString('en-IN', { month: 'long', year: 'numeric' }) %>
  </h2>

  <div class="kender-name">
    Kender: <%= kenderName || 'Not Specified' %>
  </div>

  <table>
    <thead>
      <tr>
        <th>Saadhak Name</th>
        <th>Attendance Days</th>
        <th>Kender Rank</th>
        <th>Ksheter Rank</th>
        <th>Zila Rank</th>
        <th>Mobile</th>
      </tr>
    </thead>
    <tbody>
      <% reportData.forEach(s => { %>
        <tr>
          <td><%= s.name %></td>
          <td><%= s.attendanceDays %></td>
          <td><%= s.kenderRank %></td>
          <td><%= s.ksheterRank %></td>
          <td><%= s.zilaRank %></td>
          <td><%= s.mobile %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <div class="footer">
    Generated on: <%= new Date().toLocaleDateString('en-IN', {
      day: 'numeric', month: 'short', year: 'numeric'
    }) %>
  </div>

</body>
</html>
