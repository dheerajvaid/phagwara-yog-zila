<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Missing Attendance Report</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: 'Segoe UI', 'Roboto', 'Helvetica Neue', sans-serif;
      font-size: 15px;
      color: #2c3e50;
      margin: 20px;
      background-color: #fff;
    }

    .header-logo {
      text-align: center;
      margin-bottom: 10px;
    }

    .header-logo img {
      max-height: 70px;
    }

    h2 {
      text-align: center;
      font-size: 22px;
      font-weight: bold;
      margin-bottom: 12px;
      color: #b22222; /* Dark Red */
    }

    .summary {
      font-size: 15px;
      margin-bottom: 15px;
      color: #34495e;
    }

    .summary p {
      margin: 4px 0;
    }

    .summary strong {
      display: inline-block;
      width: 140px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 10px;
    }

    th, td {
      padding: 10px 8px;
      border: 1px solid #ccc;
      text-align: left;
      font-size: 14px;
    }

    th {
      background-color: #dc3545;  /* BYS Red */
      color: #fff;
      font-weight: bold;
    }

    tr:nth-child(even) {
      background-color: #f9f9f9;
    }

    td.never {
      background-color: #fff3cd;
      color: #856404;
      font-weight: bold;
    }

    td.attended {
      background-color: #d4edda;
      color: #155724;
      font-weight: bold;
    }

    .nowrap {
      white-space: nowrap;
    }

    .footer {
      margin-top: 25px;
      text-align: right;
      font-size: 13px;
      color: #555;
    }

    @media (max-width: 600px) {
      body {
        font-size: 16px;
      }
      h2 {
        font-size: 24px;
      }
      th, td {
        font-size: 15px;
        padding: 12px 8px;
      }
      .summary {
        font-size: 16px;
      }
    }
  </style>
</head>
<body>

  <div class="header-logo">
    <img src="/images/logo.jpg" alt="Bharatiya Yog Sansthan Logo">
  </div>
  
  <h2>Missing Attendance Report</h2>

  <div class="summary">
    <p><strong>From:</strong> <%= from %></p>
    <p><strong>To:</strong> <%= to %></p>
    <p><strong>Total Missing:</strong> <%= total %></p>
  </div>

  <table>
    <thead>
      <tr>
        <th>#</th>
        <th>Name</th>
        <th>Mobile</th>
        <th class="nowrap">Last Attended</th>
      </tr>
    </thead>
    <tbody>
      <% missing.forEach((s, i) => { %>
        <tr>
          <td><%= i + 1 %></td>
          <td><%= s.name %></td>
          <td><%= s.mobile %></td>
          <td class="nowrap <%= s.lastAttended ? 'attended' : 'never' %>">
            <%= s.lastAttended 
              ? new Date(s.lastAttended).toLocaleDateString('en-IN', {
                  day: 'numeric', month: 'short', year: 'numeric',
                  timeZone: 'Asia/Kolkata'
                }) 
              : 'Never Attended' %>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <div class="footer">
    Generated on: <%= new Date().toLocaleDateString('en-IN', {
      day: 'numeric', month: 'short', year: 'numeric',
      timeZone: 'Asia/Kolkata'
    }) %>
  </div>

</body>
</html>
