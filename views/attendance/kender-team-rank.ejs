<%- include('../partials/header') %>
<%- include('../partials/navbar') %>

<section class="container mt-4">
  <h3 class="text-center mb-3 text-danger fw-bold">
    Team Attendance Rank Report – <%= selectedMonth %>/<%= selectedYear %>
  </h3>

  <div class="text-center mb-3">
    <a href="/attendance/kender-team-rank/export-pdf?month=<%= selectedMonth %>&year=<%= selectedYear %>" 
       class="btn btn-danger btn-sm fw-bold shadow">
      📄 Export as PDF
    </a>
  </div>

  <div class="table-responsive">
    <table class="table table-striped table-hover text-center align-middle">
      <thead class="table-danger">
        <tr class="fw-bold">
          <th>Saadhak Name</th>
          <th>Attendance Days</th>
          <th>Kender Rank</th>
          <th>Ksheter Rank</th>
          <th>Zila Rank</th>
          <th>Mobile</th>
        </tr>
      </thead>
      <tbody>
        <% reportData.forEach(s => { %>
          <tr>
            <td class="fw-semibold"><%= s.name %></td>
            <td class="text-success fw-bold"><%= s.attendanceDays %></td>
            <td><%= s.kenderRank %></td>
            <td><%= s.ksheterRank %></td>
            <td><%= s.zilaRank %></td>
            <td class="text-nowrap"><%= s.mobile %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</section>

<%- include('../partials/footer') %>
